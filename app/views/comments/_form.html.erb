<%= form_for([@post , @comment]) do |f| %>

	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :username %>
	<%= f.text_field :username %>

	<%= f.label :body %>
	<%= f.text_field :body %>

	<%= f.submit %>



<%end%> 